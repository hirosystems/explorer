import React from 'react';
import { Flex } from '@stacks/ui';

export const FunctionCallPanel: React.FC = props => (
  <Flex flexDirection="column" flexGrow={1} p="base" {...props}>
    FunctionCall
  </Flex>
);
